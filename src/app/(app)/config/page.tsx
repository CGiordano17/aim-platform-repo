import { NotMigrated } from "@/components/NotMigrated";

export default function ConfigPage() {
  return <NotMigrated title="Configuration" sub="Thresholds, feature flags, and privacy settings." />;
}
