import { NotMigrated } from "@/components/NotMigrated";

export default function NudgesPage() {
  return (
    <NotMigrated
      title="Nudge Library"
      sub="Skill reinforcement nudges — behavioral prompts and tool adoption questions. Not yet built as a feature (PRD build phase 3) — schema is reserved in supabase/migrations/0001_init.sql."
    />
  );
}
