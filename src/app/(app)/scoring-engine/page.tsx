import { NotMigrated } from "@/components/NotMigrated";

export default function ScoringEnginePage() {
  return <NotMigrated title="Scoring Engine" sub="Read-only view of cut-score rules and Rogers pathway thresholds." />;
}
